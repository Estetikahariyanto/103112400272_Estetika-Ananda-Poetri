#include <iostream>
#include "SLLinventory.h"  
int main() {
    linkedList L;
    createList(L);

    // === Proses Insert ===
    insertLast(L, nodePulpen, SKU-> = A001, Jumlah->20, HargaSatuan->2500 / 0%);
    insertLast(L, nodeBukuTulis, SKU-> = A002, Jumlah->15, HargaSatuan->5000 / 10%);
    insertLast(L, nodePenghapus, SKU-> = A003, Jumlah->30, HargaSatuan->1500 / 0%);

    node* delNode;
    delFirst(L, delNode, nodeAddress);
    dealokasi(delNode);

    cout << "Isi list sebelum update: ";
    printList(L); //ini menampilkan list

    // === Update data (posisi ke 2) ===
    nodeA->nama = Stabilo; sku = A010; Jumlah = 40; HargaSatuan = 9000 / 5%;
   
    cout << "Isi list setelah update: ";
    printList(L); //ini menampilkan hasil setelah update

    search(min = 2000; max = 7000);

    //bagian B
    void MaxHargaAkhir(linkedList &L)

    return 0;
}